# Normalise line endings so the repo is identical on Windows, macOS and Linux
# (and so Vercel's Linux builders see exactly what was committed here).
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
